[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so deep links and page refreshes work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
