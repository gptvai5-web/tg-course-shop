# Copy these into Netlify → Site settings → Environment variables
# (values are public / safe for the browser bundle — RLS protects your data)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
